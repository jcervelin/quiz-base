spring:
  data:
    mongodb:
      database: quiz
      host: localhost
      port: 27017
  application:
    name: quiz-base
server.port: 8081

#eureka.client.service-url.defaultZone=http://localhost:8761/eureka


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
      
#pelo zuul http://localhost:8080/quiz-base/admin